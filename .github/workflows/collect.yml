name: Market-Intel Collection

on:
  schedule:
    # Every 6 hours at :15 past the hour
    - cron: '15 */6 * * *'
  workflow_dispatch:  # manual trigger

permissions:
  contents: write   # needed to commit collected data + reports

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pyyaml

      - name: Run Market-Intel
        run: python main.py

      - name: Commit collected data + reports
        run: |
          git config user.name "Market-Intel Bot"
          git config user.email "actions@github.com"
          git add data/ reports/ actions/ 2>/dev/null || true
          if git diff --staged --quiet; then
            echo "No new data to commit"
          else
            git commit -m "Market-Intel: closed-loop run $(date -u '+%Y-%m-%d %H:%M UTC')

  Phase 4: Score → Decide → Act → Measure → Learn"
            git push
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
